This repository intentionally contains only English source code, UI strings, comments, and documentation.

The Android project uses AGP 9.0.1 with built-in Kotlin and JDK 17. GitHub Actions generates a Gradle Wrapper on the runner so the repository does not need to ship the binary wrapper JAR.
